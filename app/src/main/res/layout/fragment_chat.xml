<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/switch_track_on_color_textColor"
    android:animateLayoutChanges="true"
    android:clipToPadding="false">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvMessages"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="8dp"
        android:clipToPadding="false"
        android:contentDescription="@string/chat"/>

    <!-- ⬇️ нижний блок: прогресс СВЕРХУ, под ним панель ввода -->
    <LinearLayout
        android:id="@+id/bottomBox"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:elevation="6dp">

        <!-- прогресс НАД полем ввода -->
        <ProgressBar
            android:id="@+id/pbImage"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="4dp"
            android:indeterminate="false"
            android:max="100"
            android:progress="0"
            android:visibility="gone"
            android:progressTint="@color/backgroundDay1"
            android:progressBackgroundTint="@color/hintColor"/>

        <!-- панель ввода -->
        <LinearLayout
            android:id="@+id/inputBar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="8dp"
            android:orientation="horizontal">

            <ImageButton
                android:id="@+id/btnAttach"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:contentDescription="@string/attach"
                android:src="@android:drawable/ic_menu_add"/>

            <EditText
                android:id="@+id/etInput"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:hint="@string/type_message"
                android:imeOptions="actionSend"
                android:inputType="textMultiLine"
                android:minLines="1"
                android:maxLines="6"
                android:gravity="top|start"
                android:background="@drawable/bg_input_white_rounded"
                android:paddingStart="3dp"
                android:paddingEnd="3dp"
                android:minHeight="48dp"/>

            <ImageButton
                android:id="@+id/btnClear"
                android:layout_width="10dp"
                android:layout_height="10dp"
                android:contentDescription="@string/clear"
                android:src="@android:drawable/ic_menu_close_clear_cancel"
                android:background="@android:color/transparent"
                android:visibility="gone"/>

            <ImageButton
                android:id="@+id/btnSend"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:contentDescription="@string/send"
                android:src="@android:drawable/ic_menu_send"/>
        </LinearLayout>
    </LinearLayout>
</LinearLayout>
