<?xml version="1.0" encoding="utf-8"?>
<resources>
    <attr name="menuTintColor" format="color" />
    <!--❌ ревьюер(иначе убрать изопа en\themes + values\attr + AppTheme + Manifest)-->

    <attr name="toolbarColor" format="color" />
    <attr name="toolbarContentColor" format="color" />

    <declare-styleable name="PlaybackButtonView">
        <!-- иконки состояний -->
        <attr name="iconPlay"  format="reference"/>
        <attr name="iconPause" format="reference"/>
    </declare-styleable>

</resources>